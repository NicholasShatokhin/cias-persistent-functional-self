# Local execution outputs

`RUN_ALL_*` creates `generated/` here with frame streams, CIAS tracking, generic-estimator outputs, parameter-sensitivity results, and derived summaries.

The completed reference execution distributed with the repository is stored separately under `reference_results/additional_experiments/`.
