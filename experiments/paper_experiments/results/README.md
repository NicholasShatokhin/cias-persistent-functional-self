# Local execution outputs

`RUN_ALL_*` creates `generated/` here with run-level and frame-level outputs for the diagnostic, preflight, and confirmatory stages.

The completed reference execution distributed with the repository is stored separately under `reference_results/paper_experiments/`.
