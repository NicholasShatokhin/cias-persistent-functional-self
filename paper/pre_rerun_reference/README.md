# Compatibility documentation path

The immutable scientific core contains a historical source-code comment that points to `paper/pre_rerun_reference/supplementary.tex`. The core is kept byte-identical to the completed reference execution, so that comment is not edited. This directory preserves the referenced documentation path. The current submission manuscript is under `paper/manuscript/`.
